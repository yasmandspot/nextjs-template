import { render, screen } from '@testing-library/react';

/**
 * This is a template for creating component tests.
 * Copy this file to your component's __tests__ directory and:
 * 1. Rename 'ComponentName' to your component's name
 * 2. Import your component
 * 3. Write your tests
 *
 * Example usage:
 *
 * import { MyButton } from '../MyButton';
 *
 * describe('MyButton', () => {
 *   it('renders correctly', () => {
 *     render(<MyButton>Click me</MyButton>);
 *     expect(screen.getByRole('button')).toBeInTheDocument();
 *   });
 * });
 */

// Remove or update these example tests when using this template
describe('Component Template', () => {
  it('is just an example', () => {
    expect(true).toBe(true);
  });
});
